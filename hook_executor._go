package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"math/rand"
	"net"
	"net/http"
	"os"
	"path/filepath"
	"strings"
	"time"
)

const tcpServerAddr = "git-service:4009"
const appBuildUrl = "app-service:4005/api/apps/git/deploy"

func run() {
	in := os.Stdin
	out := os.Stdout
	info, err := readHookInput(in)
	if err != nil {
		out.WriteString("invalid hook info. failed to trigger deployment")
		return
	}
	info.NotificationKey = randString()
	data, _ := json.Marshal(info)
	t, err := newTcpClient()
	if err != nil {
		out.Write([]byte("failed to create tcp client. Internal TCP server error " + err.Error()))
		return
	}
	req, err := http.NewRequest("POST", appBuildUrl, bytes.NewReader(data))
	if err != nil {
		out.Write([]byte("failed to send build request."))
		return
	}
	if err := t.write(strings.TrimSpace(info.NotificationKey)); err != nil {
		out.Write([]byte("failed to register client " + err.Error()))
	}

	go func() {
		t.start(out)
	}()

	c := &http.Client{}
	resp, err := c.Do(req)
	if err != nil {
		out.Write([]byte("error response from API " + err.Error()))
		return
	}
	type R struct {
		Error   bool   `json:"error"`
		Message string `json:"message"`
	}
	r := &R{}
	decoder := json.NewDecoder(resp.Body)
	if err := decoder.Decode(r); err != nil {
		out.Write([]byte("failed to decode response " + err.Error()))
		return
	}
	message := ""
	if r.Error {
		message = "failed to build app: " + r.Message
	} else {
		message = "app built: " + message
	}
	out.Write([]byte(message))
}

type HookInfo struct {
	RepoName string `json:"repo_name"`
	RepoPath string `json:"repo_path"`
	OldRev   string `json:"old_rev"`
	NewRev   string `json:"new_rev"`
	Ref      string `json:"ref"`
	RefType  string `json:"ref_type"`
	RefName  string `json:"ref_name"`
	NotificationKey   string `json:"notification_key"`
}

func readHookInput(input io.Reader) (*HookInfo, error) {
	reader := bufio.NewReader(input)

	line, _, err := reader.ReadLine()
	if err != nil {
		return nil, err
	}

	chunks := strings.Split(string(line), " ")
	if len(chunks) != 3 {
		return nil, fmt.Errorf("invalid hook input")
	}
	refchunks := strings.Split(chunks[2], "/")
	dir, _ := os.Getwd()
	info := HookInfo{
		RepoName: filepath.Base(dir),
		RepoPath: dir,
		OldRev:   chunks[0],
		NewRev:   chunks[1],
		Ref:      chunks[2],
		RefType:  refchunks[1],
		RefName:  refchunks[2],
	}
	return &info, nil
}

type TcpClient struct {
	conn net.Conn
}

func newTcpClient() (*TcpClient, error) {
	conn, err := net.Dial("tcp", tcpServerAddr)
	if err != nil {
		return nil, err
	}
	return &TcpClient{conn: conn}, nil
}

func (t *TcpClient) start(fi *os.File) {
	for {
		buf := make([]byte, 1024)
		_, err := t.conn.Read(buf)
		if err != nil && err != io.EOF {
			break
		}
		fi.Write(buf[:])
	}
}

func (t *TcpClient) write(key string) error {
	m := fmt.Sprintf("%s\n", key)
	_, err := t.conn.Write([]byte(m))
	if err != nil {
		return err
	}
	return nil
}

func randString() string {
	return GenerateRandomString(10)
}

var letters = []rune("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890")
func init() { rand.Seed(time.Now().UnixNano()) }

// GenerateRandomString returns a randomly generated string
func GenerateRandomString(n int) string {
	b := make([]rune, n)
	for i := range b {
		b[i] = letters[rand.Intn(len(letters))]
	}
	return string(b)
}

func main() {
	run()
}